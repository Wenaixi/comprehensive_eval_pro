# =========================
# Comprehensive Eval Pro - 环境变量示例
# =========================
#
# 用法：
# 1) 复制本文件为 .env
# 2) 按需填写/启用变量
#
# 说明：
# - “空值”表示不启用该覆盖项，程序会继续使用 config.json 或代码默认值
# - 布尔值可写：1/0、true/false、y/n、on/off
#

# [必填(启用AI在线生成才需要)]
# SiliconFlow 文案生成 API Key。为空则只用本地缓存/默认文案，不会调用在线接口。
SILICONFLOW_API_KEY=

# [可选] 每个“任务名”单独计次：每隔 N 次提交强制生成 1 次新文案（其余走缓存）
# - 默认：3
# - <=0：永远用缓存
CEP_DIVERSITY_EVERY=3

# [可选] OCR 登录验证码最大重试次数（全程OCR）
# - 默认：10
CEP_OCR_MAX_RETRIES=10

# [可选] 默认任务选择模式（用于无人值守/批量跑；不填则走交互）
# 可用值示例：y / jx / gq / ld / bh / indices
CEP_DEFAULT_TASK_MODE=

# [可选] 配合 CEP_DEFAULT_TASK_MODE=r 或 indices 的序号列表（空格分隔，如：1 3 5）
CEP_DEFAULT_TASK_INDICES=

# [可选] 自动模式：跳过 payload 预览审查直接提交
# - 不填：仍会在批量/多任务场景提示你选择
# - 设为 1：默认开启自动模式
CEP_AUTO_MODE=

# [可选] 自动确认“重交/重复提交”提示（避免每次输入 y/n）
# - 不填：仍会提示确认
# - 设为 1：自动确认
CEP_AUTO_CONFIRM_RESUBMIT=

# [可选覆盖] 覆盖 config.json 的 model（只在配置了 SILICONFLOW_API_KEY 时用于在线生成）
CEP_MODEL=

# [可选覆盖] 覆盖 config.json 的 sso_base（SSO 登录域名）
CEP_SSO_BASE=

# [可选覆盖] 覆盖 config.json 的 base_url（业务系统 base_url）
CEP_BASE_URL=

# [可选覆盖] 覆盖 config.json 的 upload_url（图片上传接口）
CEP_UPLOAD_URL=

# [可选] 批量账号文件路径（每行：学号 空格 密码）
# - 不填默认使用项目目录下 accounts.txt
# - 可参考 accounts.example.txt
CEP_ACCOUNTS_FILE=

# [可选] 覆盖 config.json 的路径（Docker/服务器/多实例部署常用）
CEP_CONFIG_FILE=

# [可选] 覆盖 config.example.json 的路径（用于首次自动生成本地 config）
CEP_CONFIG_EXAMPLE_FILE=

# [可选] 覆盖 AI 文案缓存文件路径（默认：项目目录 content_cache.json）
CEP_CACHE_FILE=

# [可选] 覆盖验证码图片输出路径（默认：项目目录 captcha.jpg）
CEP_CAPTCHA_FILE=

# [可选] 简洁日志输出目录（每个账号 1 个文件）
# - 默认：comprehensive_eval_pro/runtime/summary_logs/
CEP_SUMMARY_LOG_DIR=
